## Agent skills

### Issue tracker

Issues live as GitHub issues in `NaruXT/videos`, via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default label vocabulary (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`), sin cambios. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout — `CONTEXT.md` + `docs/adr/` en la raíz. See `docs/agents/domain.md`.
